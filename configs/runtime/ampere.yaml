runtime:
  per_device_batch_size: 16
  grad_accum_steps: 8
  dataloader_workers: 8
  prefetch_factor: 4
  persistent_workers: true
  pin_memory: true

train:
  compile: false
  allow_tf32: true

validation:
  batch_size: 8
